# 会話決定事項メモ

作成日: 2026-06-22

このファイルは、現時点で `要件定義書.md` に正式反映していないが、会話内で固まった重要事項を一時的にまとめたメモである。

## 1. 対象・運用思想

- 対象は日本株。
- 1回の分析は1銘柄単位。ただし銘柄固定ではなく選択可能にする。
- 初期候補は、東証プライム寄りの中大型ハイテク株。
- 株価はできれば1万円以下。値嵩株は避けたい。
- 主戦場は2週間から3か月程度の中期スイング。
- 「大口に乗る」は中心思想ではなく、大口参加の兆候や質の高い材料を早めに検知できることが価値。
- 本システムは売買行動を提案するものではなく、対象銘柄に関する事実、証拠、変化、仮説への影響、不確実性、未確認情報を整理する情報提供・仮説監査システムとする。
- ポジションに応じた助言や売買行動の提案は原則行わない。
- ユーザーの判断と反省を支援するため、事実と根拠を高密度に提示することを主目的とする。
- 確定的でない助言は不要。予測を扱う場合も、「こうなったら株価が上がる可能性がある」程度の条件付き仮説として表現する。

## 1.1 不要・優先度を落とすもの

今回の方針転換により、以下は削除または優先度を落とす。

- `recommended_actions`
  - `hold_position`
  - `entry_consider`
  - `add_consider`
  - `reduce`
  - `exit_consider`
  - `no_action`
- 朝レポート・引け後レポート内の「今日やること」
- ポジション保有中の注意点を、日次レポート内でおせっかいに提示すること
- 保有継続、追加、撤退などのJudge出力
- Agentが「今取るべき行動」を出すこと
- Position画面での「今日も持つ理由」「今から新規で買いたいか」という表現
- `Chat Judge` がポジション文脈を標準で使うこと
- 「人間確認事項」という行動指示っぽい表現
- 「今日見るべき」「明日見るべき」という行動指示っぽい表現
- 自動売買に向けた要素をMVP内で前面に出すこと
- テストトレード機構を大きく作ること

代替表現:

- `recommended_actions` ではなく、`information_status`、`hypothesis_impact`、`attention_points`、`uncertainty`、`view_change_conditions` を使う。
- 「人間確認事項」ではなく、「未確認情報」「未取得情報」「情報不足」を使う。
- 「今日見るべき」「明日見るべき」ではなく、「今後の観測条件」「未確認論点」「判定変化条件」を使う。
- Position画面では、「当初仮説の現状」「保有時点から変化した事実」「撤退条件に関係する事実」「利確条件に関係する事実」「自分の判断メモ」を扱う。

追加修正:

- `conditions_to_watch` は行動指示っぽく見えるため、原則として `view_change_conditions` に置き換える。
- `view_change_conditions` の日本語表示は「見方が変わる条件」とする。
- これはユーザーに行動を促す項目ではなく、今後どの条件が観測された場合に現在の情報解釈が変わるかを示す項目である。

## 1.2 残すべきもの

今回の方針転換後も、以下は重要機能として残す。

- Evidence DB
- Evidence UI
- 証拠化Agent
- 強気Agent
- 弱気Agent
- 反証Agent
- 織り込みAgent
- Report Judge
- `direction_score`
- `confidence`
- `evidence_weight`
- `missing_information`
- `view_change_conditions`
- Chat Judge
- NotebookLM音声用Markdown
- 週次レビュー
- 後日検証
- 人間メモ

## 2. 情報源

- MVPの情報源は `J-Quants API Light + EDINET API` を中核にする。
- J-Quants Lightは定量データ基盤。株価、出来高、売買代金、財務、決算予定など。
- EDINET APIは公的開示の深掘り用。大量保有報告書、有価証券報告書、臨時報告書など。
- TradingView、有料サービス、TDnetアドオン、株探プレミアム自動取得は初期MVPでは後回し。
- 手動貼り付け運用は理想形ではない。自動取得前提で設計する。
- 要人発言、政策発言、中央銀行、政府、規制当局、主要国の経済政策など、銘柄・セクターに影響し得るマクロ/政策ニュースも情報源として扱う。
- 要人発言は、対象銘柄への直接材料ではない場合も多いため、`directness` は低めまたは中程度になることが多い。ただしセクター全体に影響する場合は `impact_level` を高く評価し得る。
- 情報種別として `macro_policy` を追加候補とする。

## 3. 情報収集構成

- 情報収集はLLM AgentがWebを広くクロールするのではなく、非LLMの取得モジュールがAPI/URLから取得する。
- 構成は以下。
  - 情報収集オーケストレーター
  - J-Quants取得モジュール
  - EDINET取得モジュール
  - IR取得モジュール
  - News取得モジュール
  - Macro/Policy News取得モジュール
- LLMが主に担当するのは、取得後の要約、分類、評価、判断。

## 4. 証拠DB

- 証拠DBは全Agentが同じ情報を読む共通土台であり、後から検証するログ。
- `source_type` は以下。
  - `price_volume`
  - `financial`
  - `market_supply`
  - `disclosure_edinet`
  - `ir`
  - `news`
  - `macro_policy`
  - `event`
- 証拠評価は4軸。
  - `source_reliability`: `A` / `B` / `C` / `D` / `E`
  - `directness`: `high` / `medium` / `low`
  - `freshness`: `high` / `medium` / `low`
  - `impact_level`: `high` / `medium` / `low`
- 方向ラベル。
  - `bullish`
  - `bearish`
  - `contradiction`
  - `priced_in`
  - `neutral`
  - `unknown`
- 人間管理項目を入れる。
  - `human_note`
  - `human_review_status`
  - `used_in_decision`
  - `review_due_date`
  - `duplicate_of`
  - `related_evidence_ids`

## 5. 保存ポリシー

- URL、タイトル、日時、要約、重要数値、短い抜粋、評価ラベルを保存。
- ニュース本文全文、有料サービス本文全文、SNS大量本文は保存しない。
- J-Quants、EDINET、Agent出力、レポートは保存対象。
- NotebookLMには本文丸コピーではなく、Agent生成レポート、証拠要約、数値、参照URLを渡す。

## 6. Agent構成

- 探索設計Agentは「今日何を見るか」固定ではなく、日次・週次・中期レビューなど、目的と期間に応じて調査設計する。
- 証拠化Agentを追加。取得素材を固定フォーマットの証拠カードに成形する。Sonnetで十分。
- 強気/弱気/反証/織り込みAgentは、与えられた視点で論理的に言える範囲だけを出す。
- 各Agentは無理に主張を作らない。証拠が弱ければ、弱い主張として出す。
- 各Agentの `claim_strength` は0から100。
- 最終方向スコア `direction_score` は裁判官Agentだけが -100 から +100 で出す。

## 6.1 MVP機能優先順位

MVPでは以下を優先する。

必須:

- Dashboard
- Evidence
- Report
- Health
- Chat

準必須:

- Position

後回し:

- Settings
- Review
- 本格的な銘柄スクリーニング
- 複数銘柄比較
- 自動売買

ChatはMVPに含める。ただし、最初から全機能を作り込むのではなく、Evidence記事の `Quick` 質問と、銘柄・レポートに対する `Context` 質問を優先する。

追加決定:

- Chat Judgeの4モード `Quick` / `Context` / `Agent` / `Research` はMVPに含める方向で進める。
- 実装上は、4モードを完全に別機能として作るのではなく、Chat Judgeのルーティングと許可範囲の違いとして実装する。
- `Research` は高負荷モードだが、MVPでは対象銘柄または入力テーマに関連する範囲に限定して実装する。

## 6.2 対象入力と自動作成モード

- ユーザーは銘柄コードを入力できる。
- レポート自動作成モードがONの時は、見るテーマも入力できるようにする。
- テーマは銘柄、セクター、業界、政策テーマ、技術テーマなど自由入力できる。
- 例:
  - `6501`
  - `半導体製造装置`
  - `AIデータセンター`
  - `電力インフラ`
  - `防衛テック`
- 自動作成モードでは、入力された銘柄・テーマをもとに、朝レポートと引け後レポートを生成する。

追加決定:

- 入力は「銘柄」または「テーマ」のどちらでもよい。
- 入力された銘柄またはテーマに関連する情報を調べる。
- 何を調べるべきかは固定リストではなく、探索設計Agentが入力内容に応じて決める。

## 7. 裁判官Agent

- 裁判官Agentが見るものは、証拠DB、各Agent出力、中期仮説、ポジション情報、前回判定。
- 裁判官Agentは新しい外部情報を直接取得しない。
- 情報不足なら `missing_information` と `next_research_tasks` を出す。
- 追加調査はMVPでは最大1回。
- 最終判定は以下。
  - `bullish`
  - `slightly_bullish`
  - `neutral`
  - `pending`
  - `slightly_bearish`
  - `bearish`
- `pending` は保留。`hold_position` は保有継続。混同しない。
- `confidence` を追加。方向スコアの大きさではなく、判定が信頼できる証拠に支えられている度合い。
  - `high`
  - `medium`
  - `low`
- 中立逃げ防止ルールを入れる。
  - 「両論あるため中立」は禁止。
  - 中立にするなら証拠重み・品質・`claim_strength` が拮抗している説明が必要。
  - 保留にするなら不足情報を具体化する。
  - `neutral_escape_check` を必須出力にする。

## 7.1 Judgeは2種類に分ける

Judge系Agentは、用途の違いにより以下の2種類に分ける。

### 7.1.1 Report Judge

通常の裁判官Agent。日次レポート、週次レポート、中期レビューなど、定型レポートの最終判定を担当する。

役割:

- 証拠DBと各Agent出力を比較する
- 最終判定 `judgement.label` を出す
- 最終方向スコア `direction_score` を -100 から +100 で出す
- `confidence` を出す
- 証拠重み `evidence_weight` を出す
- 中立逃げ検査 `neutral_escape_check` を行う
- 情報不足がある場合は `missing_information` と `next_research_tasks` を出す
- MVPでは追加調査を最大1回まで許可する

見てよいもの:

- 証拠DB
- 強気Agent出力
- 弱気Agent出力
- 反証Agent出力
- 織り込みAgent出力
- 中期投資仮説
- ポジション情報
- 前回判定

やらないこと:

- 新しい外部情報を直接取得しない
- 証拠DBにない情報を根拠にしない
- 「両論あるため中立」に逃げない
- ユーザーとの自由対話のルーティングは担当しない

### 7.1.2 Chat Judge

対話用の裁判官Agent。ユーザーからの質問を受け取り、どの情報・Agent・追加調査が必要かを判断し、根拠付きで回答する。

役割:

- ユーザー質問の意図を分類する
- 既存情報だけで答えられるか判断する
- 必要なAgentだけを呼び出す
- 必要に応じて追加調査を提案または実行する
- 回答に根拠Evidenceを添える
- 必要に応じて対話ログとして保存する

見てよいもの:

- ユーザー質問
- 現在選択中のEvidence
- 対象銘柄
- 現在のポジション情報
- 証拠DB
- 既存Agent出力
- Report Judge出力
- 過去レポート
- 人間メモ

呼び出し判断:

- 軽い質問は、既存Evidenceと既存Agentログだけで回答する
- 中程度の質問は、必要な特定Agentだけ呼ぶ
- 重い質問は、追加調査、複数Agent、Report Judge再評価を検討する
- MVPでは追加調査は最大1回までとする

質問タイプ例:

- `evidence_question`: この記事・証拠についての質問
- `judgement_question`: 判定や方向スコアについての質問
- `position_question`: 保有継続、追加、撤退についての質問
- `research_question`: 追加で何を調べるべきかの質問
- `review_question`: 過去判断やAgent精度についての質問
- `free_question`: その他

やらないこと:

- 汎用チャットの代替を目指さない
- 根拠Evidenceなしに投資判断を断定しない
- 常に全Agentを呼び出さない
- 売買指示として回答しない

基本方針:

> Report Judgeは定型レポートの最終判定者。Chat Judgeはユーザー質問に対する調査・回答の司令塔。両者は似ているが、目的と入出力が異なるため分けて定義する。

## 7.2 Chat Judgeの負荷モード

Chat Judgeは、質問の重さに応じて負荷モードを持つ。

UI上の表記は以下の4種類とする。

- `Quick`
- `Context`
- `Agent`
- `Research`

### 7.2.1 Quick

軽い質問。主に選択中の記事・証拠カードに対する理解補助。

用途例:

- この記事の意味は何か
- この証拠の弱点は何か
- この数値は何を示すか

使うもの:

- 選択中Evidence
- 関連Evidence
- 既存のAgent利用履歴

呼ばないもの:

- 新しいAgent
- 追加調査
- Report Judge再評価

Evidence記事上の `AIに質問` は、原則としてこの `Quick` を標準とする。

### 7.2.2 Context

既存文脈込みの質問。現在の銘柄、判定、ポジション、直近レポートを踏まえて答える。

用途例:

- この判定は何が根拠か
- このニュースで見方は変わるか
- 今から入るのは遅いか

使うもの:

- 証拠DB
- 既存Agentログ
- Report Judge出力
- ポジション情報
- 過去数日分のレポート

原則:

- 新しいAgentは呼ばない
- 原則、追加調査しない

### 7.2.3 Agent

特定Agentだけを呼ぶ質問。

用途例:

- 弱気Agentにこの材料を見せたらどう見るか
- 反証視点だけで見てほしい
- 織り込み済みかだけ確認したい

呼ぶ可能性があるもの:

- 強気Agent
- 弱気Agent
- 反証Agent
- 織り込みAgent

原則:

- 追加調査はしない
- Report Judge再評価は必要時のみ
- Agentモードの回答は対話中の補助判断であり、正式な日次/週次判定はReport Judgeが担当する

### 7.2.4 Research

重い調査。新しいテーマ、セクター、未監視銘柄、横断調査などに使う。

用途例:

- このセクターを調べてほしい
- AIデータセンター関連の日本株候補を見たい
- 今まで見ていなかった銘柄を調査したい

使う可能性があるもの:

- 探索設計Agent
- 情報収集オーケストレーター
- 取得モジュール
- 証拠化Agent
- 必要な分析Agent
- Report JudgeまたはChat Judgeによる統合

原則:

- 高負荷モードとして扱う
- 新規Evidenceを作成する可能性がある
- 証拠DBを更新する可能性がある
- MVPでは追加調査は最大1回までとする

## 7.3 Chat Judgeのログ保存ポリシー

Chat Judgeの対話は、モードごとに保存ポリシーを分ける。

全部を保存するとノイズになる一方、重要な対話を保存しないと後日検証できないため、以下のルールを採用する。

### 7.3.1 Quick

- デフォルトでは保存しない
- ユーザーが明示的に保存した場合のみ、Evidenceメモとして保存する
- 保存先候補: `evidence_note`
- 理由: 軽い理解補助であり、すべて保存するとノイズになるため

### 7.3.2 Context

- デフォルトでは軽量チャットログとして保存する
- 投資判断に影響した場合は `decision_note` に昇格できる
- ユーザーが保存しない / メモ保存 / 判断ログに保存を選べる
- 保存先候補: `chat_log` / `evidence_note` / `decision_note`
- 理由: 判断に関わる可能性があるが、すべてを正式ログにすると重いため

### 7.3.3 Agent

- デフォルトで保存する
- 保存先候補: `chat_log` / `agent_run` / `evidence_note`
- 理由: 新しいAgent解釈が生成されるため、後から検証価値があるため
- 注意: Agentモードの出力は正式なReport Judge判定を上書きしない

### 7.3.4 Research

- 必ず保存する
- ユーザーによる保存オフは原則不可
- 保存先候補:
  - `research_plan`
  - `raw_collection_log`
  - `created_evidence`
  - `agent_run`
  - `chat_log`
  - `report`
- 理由: 新規情報を取得し、証拠DBを更新する可能性があるため、保存しないと再現不能になるため

固定値案:

- `save_level`
  - `none`
  - `chat_light`
  - `decision_note`
  - `agent_run`
  - `research_full`
- `save_targets`
  - `chat_log`
  - `evidence_note`
  - `decision_note`
  - `agent_run`
  - `research_plan`
  - `created_evidence`
  - `report`

推奨ルール:

| Mode | default_save | save_level | user_can_override |
|---|---:|---|---:|
| Quick | false | none | true |
| Context | true | chat_light | true |
| Agent | true | agent_run | true |
| Research | true | research_full | false |

## 8. 裁判官JSONで決めた追加項目

- `decision_stage`
- `position_context`
  - ただし、標準レポートで売買助言に使わない。ユーザーが明示的にポジション文脈を求めた時、またはReview/Position用途で参照する。
- `confidence_detail`
- `evidence_weight`
- `agent_inputs`
- `decision_reasoning`
- `risks`
- `view_change_conditions`
- `neutral_escape_check`
- `research_control`
- `used_evidence`
- `review_schedule`
- `missing_information`
- `warnings`
- `audit`

削除・置換:

- `recommended_actions` は削除する。
- `conditions_to_change` は `view_change_conditions` に置き換える。
- `human_check_required` は、行動指示っぽいため `missing_information`、`uncollected_information`、`uncertainty.factors` へ置き換える。

## 8.1 Report Judgeの情報整理項目

Report Judgeは売買行動の提案ではなく、以下の情報整理項目を出力する。

### 8.1.1 information_status

情報状態。現在の証拠がどの方向に偏っているか、または情報不足かを示す。

固定値案:

- `bullish_evidence_leading`: 強気材料優勢
- `bearish_evidence_leading`: 弱気材料優勢
- `mixed`: 材料拮抗
- `insufficient_information`: 情報不足
- `no_material_change`: 大きな変化なし

### 8.1.2 hypothesis_impact

中期投資仮説への影響を示す。

固定値案:

- `supportive`: 補強
- `slightly_supportive`: やや補強
- `neutral`: 中立
- `slightly_negative`: やや悪化
- `negative`: 悪化
- `undetermined`: 判定不能

### 8.1.3 uncertainty

不確実性。未確認情報、情報品質、直接性不足などを示す。

固定値案:

- `high`
- `medium`
- `low`

### 8.1.4 view_change_conditions

見方が変わる条件。

これは「見てください」という行動指示ではなく、今後どの条件が観測された場合に、現在の情報解釈、証拠重み、仮説への影響が変わるかを示す。

例:

- 出来高増加後に価格が維持できない場合、強気材料の重みは下がる
- 競合企業も同領域の需要改善を示す場合、中期仮説の補強度は上がる
- 信用買い残が急増している場合、過熱リスクの重みは上がる

`view_change_conditions.effect` は固定値化せず、自由文で記録する。

## 9. Position / テストトレード

- 別の大きなテストトレード機構を作るより、Position記録で実/仮想/監視を扱う。
- `position_type`
  - `real`
  - `paper`
  - `watch`
- `position_mode`
  - `realistic`
  - `training`
- 初期段階では、大きなテストトレード機構は作らない。
- テストトレードは、ユーザーが架空のポジションを報告し、それをPosition記録として扱うことで対応する。
- `training` では部分利確、買い増し、分割撤退などを記録できる余地を残すが、MVPでは前面に出さない。
- Position情報は、売買助言のためではなく、判断ログ・事後検証・反省のために使う。

## 10. Evidence UI

- Evidenceは単なるDB管理画面ではなく、1銘柄専用の高密度ニュースサイト。
- JSONは裏側。人間は証拠カードとして読む。
- 一覧ではタイトル、要約、重要数値を主役にする。
- IDや取得元などの技術情報は極小表示。
- フィルタは常時左側を占有せず、折りたたみ式。
- 記事は一覧プレビューと全画面/リーダーモードを分ける。
- 各記事からAIに質問できる。
- AI質問ボタンは最終的には `?` ではなく、一目でAI呼び出しと分かる表示にする。

## 11. レポート出力

- 出力はMarkdownとHTMLの2系統。
- MarkdownはLLM/NotebookLM向け。
- HTMLは人間がUIで読む視覚的レポート。
- Markdownは2種類に分ける。
  - 詳細分析用Markdown
  - NotebookLM音声用Markdown
- NotebookLMには、できれば自動投入したい。
- MVPの最低ラインは、Google Docs/Driveへの自動保存とする。
- Markdown生成とUI上のコピー/ダウンロードも残す。
- NotebookLMへの完全自動投入は、公式APIや利用可能な連携手段、規約、安定性を確認した上で実装する。
- 将来的にGoogle Docs/Drive連携、NotebookLM API、ブラウザ操作を検討。

## 11.1 生成タイミング

朝レポート:

- ユーザー起床前に生成完了していることを目標とする。
- 生成完了目標は朝7:00までとする。
- 朝レポートは、起床後に音声で動向を把握するための短時間ブリーフとする。
- 朝レポートは、入力された銘柄またはテーマに関連する情報を対象にする。

引け後レポート:

- 日本株の引け後、概ね16:00から17:30頃の生成を想定する。
- 引け後レポートは、その日の事実、値動き、重要証拠、情報状態、仮説への影響、不確実性を整理する正式な日次情報ログとする。

取得頻度:

- MVPでは、朝1回、引け後1回、必要に応じた手動更新を想定する。
- 中期スイング前提のため、秒単位の高頻度監視は不要。

## 11.2 Review優先度

- Review画面はMVPでは不要。
- 週次レビューや本格Reviewは後続フェーズで再ヒアリングする。

## 12. 作成済みプロトタイプ

- `Agent構成まとめ.html`
- `Evidence画面プロトタイプ.html`
- `全体構成図.html`

## 12.1 MVP縦スライス実装済み

以下を、サンプルJSONから再生成できるMVPの最初の画面群として実装した。

- `scripts/generate_reports.py`
  - Evidence、Agent出力、Report Judge出力から、詳細Markdown、NotebookLM音声用Markdown、HTMLレポートを生成する。
- `scripts/generate_app_pages.py`
  - Dashboard、Evidence、Health、Agent指示書一覧を生成する。
- `app/dashboard.html`
  - 情報状態、仮説への影響、方向スコア、不確実性、重要Evidence、Agentの見方、Chat Judgeモード、Health概要を表示する。
- `app/evidence.html`
  - Evidenceカードを高密度ニュース画面として表示する。
  - フィルタは折りたたみ式にし、通常時は記事面を広く使う。
  - 記事ごとにAI質問導線を置く。現時点ではUI導線のみ。
- `app/health.html`
  - 情報取得、証拠化、Agent、Judge、出力、NotebookLM/Drive連携の状態を表示する。
  - エラーや未接続項目はUI上で見えるようにする。
- `app/agents.html`
  - 各Agentの `CLAUDE.md` 指示書へ移動できる一覧。
- `agents/*/CLAUDE.md`
  - 探索設計、証拠化、強気、弱気、反証、織り込み、Report Judge、Chat Judgeの指示書。

この段階では実データ取得APIやChat Judge実回答は未接続。
ただし、情報が入った後にEvidence、Agent、Judge、Report、Dashboard、Healthへ流れる画面上の骨格は接続済み。

## 13. Health / エラー表示

エラー時の出力はあらかじめ決めておく必要がある。

基本方針:

- エラーはUI上に必ず表示する。
- MVPでは、エラー通知はUI表示のみとする。
- メール、Slack、LINEなどの外部通知は初期MVPでは不要。
- ユーザー向けには、何が失敗したか、どのレポートや証拠に影響するか、代替出力があるかを表示する。
- 内部ログには、失敗した処理、時刻、対象銘柄、原因、再実行可否を保存する。

Health画面に出す主な項目:

- J-Quants取得成功/失敗
- EDINET取得成功/失敗
- IR取得成功/失敗
- News取得成功/失敗
- Macro/Policy News取得成功/失敗
- 証拠化成功/失敗
- Evidence DB更新件数
- Agent出力成功/失敗
- Report Judge出力成功/失敗
- Chat Judge出力成功/失敗
- レポート生成成功/失敗
- NotebookLM用Markdown生成成功/失敗
- NotebookLM/Google Docs/Drive連携成功/失敗

ユーザー向けエラー表示の例:

```text
J-Quants取得に失敗しました。
価格・出来高データが未更新のため、本日の方向スコアと証拠重みは前回データを含む可能性があります。
IR/News/EDINETの取得は成功しています。
```

エラー時のレポート方針:

- 取得失敗があっても、可能な範囲でレポートを生成する。
- ただし、どの情報が欠けているかを明記する。
- 欠損が重大な場合は、`information_status` を `insufficient_information` に寄せる。
- `confidence` は必要に応じて下げる。
