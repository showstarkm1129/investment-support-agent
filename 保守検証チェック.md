# 保守検証チェック

このプロジェクトでは、画面やJSONを変更した後に静的検証を実行する。
外部APIには接続せず、ローカルファイルだけで「壊れていないか」を確認する。

## 実行方法

```bash
python3 scripts/validate_static.py
```

生成物の古さチェックを省略したい場合:

```bash
python3 scripts/validate_static.py --skip-generated
```

## チェック内容

- 必須ファイルが存在するか
- Pythonスクリプトの構文が壊れていないか
- サンプルJSONが読めるか
- Evidence IDが重複していないか
- Agent出力がEvidence IDを正しく参照しているか
- Report Judgeの `used_evidence` と `view_change_conditions` が存在するEvidenceを参照しているか
- Healthの件数が実データと矛盾していないか
- HTMLのローカルリンクが切れていないか
- Dashboard/Evidence/Health/Agentsなどの生成HTMLが最新か
- 売買行動提案に見える禁止表現が混入していないか

## 使いどころ

- Evidence JSONやAgent出力JSONを手で直した後
- DashboardやHealth画面の生成ロジックを変えた後
- Agent指示書を追加・移動した後
- レポート生成スクリプトを変更した後
- 実API接続前に、既存のMVP骨格が壊れていないか確認したい時

このチェックは投資情報の正しさを保証するものではない。
あくまで、ファイル構造、参照関係、生成物、保守ルールが壊れていないかを見るための機構。
